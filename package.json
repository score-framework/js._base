{
  "name": "score.init",
  "version": "0.0.3",
  "description": "Initializer for the javascript part of The SCORE Framework.",
  "main": "init.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "pretest":"[[ -f \"test/index.html\" ]] || (git submodule init && echo \" ==> Tests set up\";echo);git submodule update",
    "preinstall": "git submodule init",
    "test": "$(node -e \"console.log(require('path').dirname(require.resolve('mocha')) + '/bin/mocha')\")"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/score-framework/js.init.git"
  },
  "keywords": [
    "score",
    "framework",
    "init",
    "module"
  ],
  "author": "strg.at <score@strg.at>",
  "license": "LGPL-3.0",
  "bugs": {
    "url": "https://github.com/score-framework/js.init/issues"
  },
  "homepage": "https://github.com/score-framework/js.init#readme",
  "devDependencies": {
    "expect.js": "0.3.x",
    "mocha": "2.x.x",
    "sync-request": "^3.0.1"
  }
}
